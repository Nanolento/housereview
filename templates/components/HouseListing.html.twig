<div>
  <h2>{{ title }}</h2>
  <p>Title Grade: {{ titleGrade }}</p>
  <p>Rent: {{ monthlyRent }} ({{ rentGrade }})</p>
  <p>City: {{ city }}</p>
  <p>Energy Label: {{ energyLabel }} ({{ energyGrade }})</p>
  <p>Overall Grade: {{ overallGrade }}</p>
</div>

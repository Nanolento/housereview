<div {{ attributes }}>
  <h2>{{ title }}</h2>
  <p>Title Grade: {{ titleGrade.value }}</p>
  <p>Rent: {{ monthlyRent }} ({{ rentGrade.value }})</p>
  <p>City: {{ city }}</p>
  <p>Energy Label: {{ energyLabel }} ({{ energyGrade.value }})</p>
  <p>Overall Grade: {{ overallGrade.totalScoreLabel }}</p>
  <p>Status: {{ status.value }}</p>
</div>
